{
    "name": "@wikione/auth-core",
    "version": "0.1.0",
    "private": true,
    "description": "Framework-independent password and encrypted-session primitives for WikiOne",
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
