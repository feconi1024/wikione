{
    "name": "@wikione/auth-store",
    "version": "0.1.0",
    "private": true,
    "description": "PostgreSQL account and encrypted Redis session adapters for WikiOne",
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@wikione/auth-core": "workspace:*",
        "postgres": "^3.4.0",
        "redis": "^5.0.0"
    }
}
