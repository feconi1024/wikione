import type {
    NormalizedPublishError as ContractPublishError,
    WatchlistBehavior as ContractWatchlistBehavior,
} from '@wikione/contracts';

export type NormalizedPublishError = ContractPublishError;
export type WatchlistBehavior = ContractWatchlistBehavior;
